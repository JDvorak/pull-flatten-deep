{
  "name": "pull-flatten-deep",
  "version": "1.0.0",
  "description": "Pull-stream which recursively flattens streams of streams",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps && NODE_ENV=test node test",
    "test:cov": "standard && npm run deps && NODE_ENV=test istanbul cover test.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jdvorak/pull-recursive-flatten.git"
  },
  "keywords": [
    "pull-stream",
    "recursive flatten",
    "flatten all",
    "flatten",
    "streams"
  ],
  "license": "MIT",
  "dependencies": {
    "pull-stream": "^3.5.0"
  },
  "devDependencies": {
    "dependency-check": "^2.8.0",
    "istanbul": "^0.4.5",
    "standard": "^10.0.1",
    "tape": "^4.6.3"
  },
  "bugs": {
    "url": "https://github.com/jdvorak/pull-recursive-flatten/issues"
  },
  "homepage": "https://github.com/jdvorak/pull-recursive-flatten#readme",
  "author": "John Dvorak <john@johnsdvorak.com>"
}
